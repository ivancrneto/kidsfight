// Mock for image and asset imports in tests
const path = require('path');

// Return the filename as the mock value
module.exports = 'test-file-stub';

// For ES modules
export default 'test-file-stub';